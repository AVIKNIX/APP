# APP

This is a simple login demonstration built with HTML, CSS and JavaScript. The `public` directory contains the source files:

- `index.html` – the login form
- `style.css` – basic styling
- `script.js` – frontend logic validating credentials

To try it out, open `public/index.html` in your browser. Login with username `admin` and password `password`.
